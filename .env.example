# ============================================================================
# AxiomOS Labs 环境配置示例 (.env)
# ----------------------------------------------------------------------------
# 复制此文件为 .env 并根据实际情况修改各配置项。
# 所有配置项均支持环境变量覆盖，优先级：命令行 > .env > 默认值。
# ----------------------------------------------------------------------------
# 1. LLM 配置（必需）
# ----------------------------------------------------------------------------

# DeepSeek API 密钥（必需）
# 获取地址：https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# DeepSeek API 基础 URL（默认即可）
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 使用的模型名称（默认 deepseek-chat，可选 deepseek-reasoner 等）
DEEPSEEK_MODEL=deepseek-chat

# ----------------------------------------------------------------------------
# 2. Fast‑Downward 规划器配置（必需）
# ----------------------------------------------------------------------------

# Fast‑Downward 可执行文件路径
# 项目已包含 downward/ 子模块，编译后自动生成 fast-downward.py
# 若使用自定义路径，请填写绝对路径或相对于项目根目录的路径
DOWNWARD_PATH=./downward/fast-downward.py

# ----------------------------------------------------------------------------
# 3. MCP（Model Context Protocol）配置
# ----------------------------------------------------------------------------

# 是否启用 MCP 执行器（true/false）
# 启用后，技能将通过 MCP 服务器远程调用；禁用则使用本地技能执行器
USE_MCP=true

# MCP 服务器启动命令（默认 python3）
MCP_SERVER_COMMAND=python3

# MCP 服务器启动参数（默认启动项目内置的结构化服务器）
# 多个参数用空格分隔，例如 "mcp_server_structured.py --port 8080"
MCP_SERVER_ARGS=mcp_server_structured.py

# ----------------------------------------------------------------------------
# 4. 运行参数配置
# ----------------------------------------------------------------------------

# 最大迭代次数（每次任务规划‑执行循环的最大轮数）
MAX_ITERATIONS=5

# 进化算法最大重试次数（技能进化失败后的重试上限）
MAX_EVOLUTION_RETRIES=4

# 规划器超时时间（秒）
PLANNING_TIMEOUT=30

# ----------------------------------------------------------------------------
# 5. 训练模式专用配置（仅在使用 auto_trainer.py 时生效）
# ----------------------------------------------------------------------------

# 训练轮数（自主学习模式下的训练回合数）
TRAINING_ROUNDS=1

# ----------------------------------------------------------------------------
# 6. 高级配置（一般无需修改）
# ----------------------------------------------------------------------------

# 项目根目录（自动检测，无需手动设置）
# PROJECT_ROOT=/path/to/AxiomOS

# 存储路径（工作区目录，默认 ./workspace）
# STORAGE_PATH=./workspace

# 测试路径（默认 ./tests）
# TESTS_PATH=./tests

# 沙盒运行路径（默认 ./sandbox_runs）
# SANDBOX_RUNS_PATH=./sandbox_runs

# 技能目录路径（默认 ./infrastructure/skills）
# SKILLS_PATH=./infrastructure/skills

# 临时目录路径（默认 ./.temp）
# TEMP_DIR=./.temp

# ----------------------------------------------------------------------------
# 7. 日志与调试配置
# ----------------------------------------------------------------------------

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
# LOG_LEVEL=INFO

# 是否启用彩色日志输出（true/false）
# COLOR_LOG=true

# 是否输出详细调试信息（true/false）
# VERBOSE=false

# ============================================================================
# 注意事项
# ============================================================================
# 1. 必须设置 DEEPSEEK_API_KEY 和 DOWNWARD_PATH（若未编译 Fast‑Downward 需先编译）。
# 2. 若使用 MCP 模式，请确保已安装 mcp 包（pip install mcp）。
# 3. 路径配置可使用绝对路径或相对于项目根目录的路径。
# 4. 生产部署建议将敏感信息（如 API 密钥）通过环境变量传递，而非写入 .env 文件。
# ============================================================================